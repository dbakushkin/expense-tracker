// DTO и общие типы между @expence-tracker/web и @expence-tracker/api.
// Добавляйте сюда контракты API (request/response), доменные типы (Expense,
// Category, User), enum-ы статусов и валют. Не импортируйте сюда runtime-код
// (Prisma client, NestJS, React) — только чистые типы.

export * from "./auth";
